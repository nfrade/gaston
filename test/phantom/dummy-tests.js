describe('some tests to see if the tester is working', function(){

  it('should do some stuff', function(){
    assert.equal(1, 0);
  });

  it('should do some stuff', function(){
    assert.equal(1, 0);
  });

  it('should do some stuff', function(){
    assert.equal(1, 1);
  });

});