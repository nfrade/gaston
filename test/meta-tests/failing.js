describe('some tests to see if the tester is working', function () {
  it('should fail', function () {
    assert.equal(1, 0)
  })

  it('should fail', function () {
    assert.equal(1, 0)
  })

  it('should fail', function () {
    assert.equal(1, 0)
  })

  it('should pass', function () {
    assert.equal(1, 1)
  })
})